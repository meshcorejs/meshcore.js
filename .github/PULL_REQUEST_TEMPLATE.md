## What

<!-- What changes and why. -->

## Checklist

- [ ] `pnpm check`, `pnpm typecheck`, `pnpm test`, `pnpm build` pass
- [ ] Tests cover the change
- [ ] `AGENTS.md` of the touched packages updated when the public API, layout or rules changed
- [ ] Changeset added (`pnpm changeset`) when a published package changed
